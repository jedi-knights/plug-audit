//! Domain layer — pure types with no I/O.
//!
//! Populated in PA-2 (rule-name ontology + finding format). This module
//! deliberately holds only the module declarations until then so that
//! the workspace compiles.

// PA-2 targets:
// pub mod categories;
// pub mod finding;
// pub mod rule;
// pub mod rule_engine;
// pub mod severity;
